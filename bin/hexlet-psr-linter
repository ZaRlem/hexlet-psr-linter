#!/usr/bin/env php

<?php

require_once __DIR__ . '/../vendor/autoload.php';

use function HexletPsrLinter\lint;
use function HexletPsrLinter\getFilePath;

$pathArray = getFilePath();
if($pathArray[0]){
    $code = file_get_contents($pathArray[0]);
}

$result = lint($code);
var_dump($result);
